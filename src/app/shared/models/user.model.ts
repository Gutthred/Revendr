export interface User {
    id: number | null,
    fname: string | null,
    lName: string | null,
    email: string | null,
    password: string | null,
    company: number | null,
    user: string | null,

}